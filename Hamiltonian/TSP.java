/*
 * Jerry Huang
 * 
 * TSP implementation.
 * Solves the traveling salesman problem for a set of points
 */
public class TSP {
	
	
	// Empty constructor
	public TSP() {
	}
	
	
	/**
	 * Determines whether there is a traveling salesman path through all the points
	 * 
	 * @param points to be in TSP
	 * @return whether there is a path or not
	 */
	public boolean hasPath(MyPoint[] points) {
		// Add implementation code
	}
	
	
	/**
	 * Returns the traveling salesman solution if there is a path
	 * 
	 * @param points to be in TSP
	 * @return TSP path containing points
	 */
	public MyPoint[] getPath(MyPoint[] points) {
		// Add implementation code
	}

}
